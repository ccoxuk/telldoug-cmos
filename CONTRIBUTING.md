# Contributing (MVP Guardrails)

## Merge discipline
- No direct pushes to `main`.
- Squash merges only.
- CI must be green before merge.

## Scope discipline
- Do not add AI/import behavior in MVP (must remain disabled).
- Migrations are manual only (never on boot).
